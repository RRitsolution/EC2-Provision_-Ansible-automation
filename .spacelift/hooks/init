#!/bin/bash
set -e

echo "🔧 [HOOK] Starting Spacelift init hook..."

# Install pip if missing
if ! command -v pip &> /dev/null; then
  echo "⚠️ pip not found, installing..."
  curl -sS https://bootstrap.pypa.io/get-pip.py | python3
fi

echo "✅ pip found at: $(which pip)"
echo "📦 Installing boto3 and botocore globally..."

pip install boto3 botocore

echo "✅ boto3 and botocore installed successfully"
